{% extends 'base.html.twig' %}

{% block title %}- Nos biens gérés{% endblock %}

{% block body %}

<div class="container mt-4">
    <h1>Les biens</h1>
     <div class="row flex">
    {% for property in properties %}
        {% include 'manager/property/_property.html.twig' %}
    {% endfor %}
    </div>
    {# display navigation #}
    <div class="navigation">
        {{ knp_pagination_render(properties) }}
    </div>
</div>  


{% endblock %}