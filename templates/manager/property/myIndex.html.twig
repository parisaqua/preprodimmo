{% extends 'base.html.twig' %}

{% block title %}- Gérer les biens{% endblock %}

{% block body %}


<div class="container mt-4">
    
    <div class="row align-items-center mb-3">
        <div class="col-md-8">
            <h2 class="text-primary"><i class="fas fa-home"></i>&nbsp;Mon portefeuille</h2>
        </div>
        <div class="col-md-4">
            <a href="{{ path('property.manager.new')}}" class="btn btn-info btn-right mb-2"><i class="fas fa-plus"></i>&nbsp;Ajouter un bien</a>
        </div>
    </div>
   
  
    
     <div class="row flex">
    {% for property in properties %}
        {% include 'manager/property/_property.html.twig' %}
    {% endfor %}
    </div>
    {# display navigation #}
    <div class="navigation">
        {{ knp_pagination_render(properties) }}
    </div>






    {# display navigation #}
    {# <div class="navigation">
        {{ knp_pagination_render(properties) }}
    </div> #}

    <div class="row">
        
        {% if properties|length < 1 %}
            
            <div class="col-md-12 text-right">
                
                    <img src="{{  '/images/firstmessage7.png' }}" alt="Vous n'avez aucun bien d'enregistrer. Ajoutez un bien !" width="100%" >
                    {# <h4 class="newMessage">Vous n'avez aucun bien d'enregistrer. Ajoutez un bien !</h4> #}
                     <div class="row align-items-center">
                        <div class="col">
                            <hr class="" style="width: 100%; border-bottom: 5px solid #446E9B">
                        </div>
                        <div class="col-auto">
                            <img src="{{  '/images/logo-menu-ID.png' }}" alt="" width="80" height="auto">
                        </div>
                     </div>
                     
                   
               
            </div>
           
        {% endif %}
        
    </div>

</div> 

{% endblock %}