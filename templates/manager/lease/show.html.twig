{% extends 'base.html.twig' %}

{% block title %}Bail{% endblock %}

{% block body %}
    <h1>Bail</h1>

    

    <a href="{{ path('lease.manager.index') }}">back to list</a>

    <a href="{{ path('lease.manager.edit', {'id': lease.id}) }}">edit</a>

    {{ include('manager/lease/_delete_form.html.twig') }}
{% endblock %}
