{% extends 'base.html.twig' %}

{% block title %}Demande de réinitialisation de mot de passe{% endblock %}

{% block body %}

<div class="container">
    <h1 class="mt-5 mb-4"><i class="fas fa-user-lock"></i>&nbsp;&nbsp;Renouvellement du mot de passe.</h1>
     {{form_start(form)}}
    <div class="alert alert-light py-3 px-3">
        <div class="row">
            <div class="col-md-1"></div>
            <div class="col-md-5">
                {{form_row(form.hash.first)}}
            </div>
            <div class="col-md-5">
                {{form_row(form.hash.second)}}
            </div>
            <div class="col-md-1"></div>
        </div>
        <div class="row mt-2 pt-3">
            <button class="btn btn-success btn-center"  type="submit"><i class="fas fa-check"></i>&nbsp;Envoyer</button>
        </div>
    </div>
    {{form_end(form)}}

</div>


{% endblock %}
